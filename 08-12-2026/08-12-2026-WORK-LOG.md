# August 12, 2026 work log

This file records what went into the comparison package and which results were reused from the earlier audits. The distinction matters: assembling a reproducible report is not the same as rerunning every software test.

## 1. Set the comparison points

I used the exact old and new commits rather than comparing whatever happened to be checked out most recently.

- Old audited snapshot: `07aee813375347bcc933ad21b4aed561dd7cd3bf`.
- Old distribution/runtime versions: `2.0.2` and `2.0.0`.
- New snapshot: `d4d45decec56e1ebec15cf04ae62ff944851780e`.
- New package/runtime version: `3.1.0`.

The source diff spans 88 files, 21,134 additions, and 10,325 deletions. This is why the old private-function tests cannot simply be run unchanged against v3.

## 2. Reuse the August 10 finding list

The original audit produced 239 test-case outcomes but 86 unique findings: 79 reproduced bugs and 7 feature gaps. I copied the original `BUGS.json` into `data/august-10-findings.json` and kept its IDs, priorities, summaries, and test names intact.

## 3. Use the saved v3 evidence

The v3 evidence was generated from the August 11 source checkout. I copied the machine-readable status mapping, the final upstream JUnit file, and its text summary into this dated package. The final v3 run reports 327 collected tests, 321 passed, 6 skipped, and 0 failed. The earlier restricted-cache preflight is not used as the final result.

I did not rewrite the original August 11 audit report. It is included as `reports/V3-ORIGINAL-CHANGE-AUDIT.md` so its source date and wording remain traceable.

## 4. Classify the same 86 IDs

The build script joins each old finding ID to exactly one of the v3 lists: fixed, still reproducible, changed, or not retested. It stops if an ID is missing, duplicated, or new. The resulting totals are:

- 60 fixed;
- 19 still reproducible;
- 7 changed interface/behavior;
- 0 not retested.

The P1/P2-P3 split is also recalculated from the original priorities: P1 is 14/5/1, and P2/P3 is 46/14/6.

## 5. Check the frozen pathway evidence

The old and new hsa04110 XML and PNG files have matching SHA-256 hashes. The saved old and new raw half-and-half overlays are both 1039 × 801. The comparison figure reads those saved images and measures their dimensions and pixel differences; it does not rerun both software versions. The input values are equivalent across the two saved runs: the same three Entrez IDs and numbers, with different condition-column names.

## 6. Build the written and machine-readable outputs

The package contains a summary report, a full 86-row index, separate fixed/remaining/changed reports, CSV and JSON tables, two status charts, a labeled saved-image comparison, and a selected proof folder. The figures copied from the older folders retain their original provenance in the handoff index.

## 7. Execute the August 12 notebook

The notebook checks the ID join, status counts, priority split, remaining P1 list, source-file hashes, JUnit totals, and complete CSV table. Its saved execution report records 12 code cells executed, 0 unexecuted cells, and 0 error outputs. It does not claim that all 86 behaviors were newly rerun on August 12.

## 8. Scope of changes

No Pathview Plus source file was changed. The work here changes only the comparison package: explanatory Markdown, report tables, chart labels, copied evidence, and the scripts that assemble those artifacts.
